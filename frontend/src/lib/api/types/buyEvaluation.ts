/**
 * Generated by orval v6.31.0 🍺
 * Do not edit manually.
 * Momentum Dashboard API
 * Contract-first spec. Clients must be generated from this file. All endpoints are local-only and authenticated as N/A for now.
 * OpenAPI spec version: 0.8.0
 */
import type { BuyCheck } from './buyCheck';
import type { BuyEvaluationMode } from './buyEvaluationMode';

export interface BuyEvaluation {
  /** Checklist rows for the enforced gates. */
  checks: BuyCheck[];
  /**
   * Snapshot of enforced gate codes applied during evaluation.
   * @nullable
   */
  enforced_checks?: string[] | null;
  /**
   * Timestamp when the buy evaluation was performed.
   * @nullable
   */
  eval_ts?: string | null;
  /** Codes of the enforced gates that failed. */
  failed_codes: string[];
  /** True when all enforced buy gates passed. */
  flag: boolean;
  /**
   * Evaluation mode used for the buy gates.
   * @nullable
   */
  mode: BuyEvaluationMode;
  /** Number of enforced gates that passed. */
  pass_count: number;
  /**
   * Active buy profile key (e.g., swing_eod, intraday_breakout).
   * @nullable
   */
  profile: string | null;
  /**
   * Inline summary of the buy reasons.
   * @nullable
   */
  reasons_inline?: string | null;
  /** Total enforced gates evaluated. */
  total_count: number;
}
